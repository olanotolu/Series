# Audio Files

Voice memos from users are saved here.

- `.opus` files: Original OPUS format (smallest size)
- `.wav` files: Converted WAV format (for easy playback)

Files are named: `voice_memo_{phone}_{timestamp}_{message_id}.{ext}`
